<ContentDialog
    x:Class="随机抽取学号.Views.ClassManagerContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:communitytoolkitcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:随机抽取学号.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="TITLE"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    PrimaryButtonText="Button1"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    SecondaryButtonText="Button2"
    mc:Ignorable="d">

    <Grid>
        <communitytoolkitcontrols:Segmented HorizontalAlignment="Stretch">
            <communitytoolkitcontrols:SegmentedItem Content="班级图片" FontFamily="{StaticResource HarmonyOSSans}" />
            <communitytoolkitcontrols:SegmentedItem Content="班级名称" FontFamily="{StaticResource HarmonyOSSans}" />
        </communitytoolkitcontrols:Segmented>
        <Grid>
            <Grid>
                <PersonPicture ProfilePicture="/Photos/background.jpg" />
            </Grid>
            <Grid />
        </Grid>
    </Grid>
</ContentDialog>
